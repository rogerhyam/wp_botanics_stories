jQuery(document).ready(function(){
   

	// spot anything tagged with X and add i
	
	// work through all the tags and add them as classes to the article
	// so we can style them at will!
	
	//jQuery("a[rel='tag']").parents("article").addClass('banana'); 

});